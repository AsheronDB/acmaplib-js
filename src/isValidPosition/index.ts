import type { Position } from "../classes";

export const isValidPosition = function (position: Position): boolean {
  // Check is typeof Position

  // Check min/max landblock range

  // Check min/max origin values

  // Check quaternion validity

  return true;
};
